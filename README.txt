Student score portal demo - pass 11 safe

Updates:
- changed the top line to black
- moved the right-side dashboard sections farther right
- added extra spacing/overflow protection so the score-details circle section should stop getting cropped

Run:
npm install
npm run dev
